for nested navigation

first create sealed class for all the screen info

then create different class for accessing auth navigation and app navigation as nestedGraph

when user first visit to our app we will navigate to the authGraph

=>>> in auth graph he will navigate to all the authentication ui s
=>>> after authentication we set the navigate to appNavigation >> after that he will use our app features

=>>> after that we check is the user logged in ? navigate to appNavGraph : navigate to AuthNavGraph